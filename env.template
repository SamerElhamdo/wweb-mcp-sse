# Required: Authentication token for all endpoints
AUTH_TOKEN=change-me-very-secret

# Optional: Server configuration
PORT=3000
LOG_LEVEL=dev

# Optional: Webhook configuration
WEBHOOK_URL=
WEBHOOK_SECRET=

# Optional: Session directory (default: /data/session in container)
WWEB_SESSION_DIR=/data/session
